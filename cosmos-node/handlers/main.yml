---
# Handlers for cosmos-node role

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: restart cosmos service
  ansible.builtin.service:
    name: "{{ service_name }}"
    state: restarted
